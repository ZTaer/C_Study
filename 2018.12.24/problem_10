0. 请问 char *name = “FishC” 和 char name[] = {‘F’, ‘i’, ‘s’, ‘h’, ‘C’} 是否等价？

不等价,*name只能使用%c输出,而name[]可以使用%s或%c

name[]在加个'\0'

1. 请解释为何下边变量 a 的值为何打印出来是 144（请写出分析过程）？
	
	哭.......
	200(10) == 128(10) + 64(10) + 8(10) == 1000 0000(2) + 100 0000(2) + 1000(2) == 1100 1000(2)1100 1000(2) + 1100 1000(2) == 1 1001 0000(2)
由于 char 类型占一个字节（8 位），因此舍弃掉最前边的进位，结果为 1001 0000(2) == 144

2. 请将语句 a = b = c = 3; 拆分成 3 条独立语句。

	c = 3;
	b = c;
	a = b;

3. 你听说过“表达式”，也听说过“语句”，那请问两者的区别是什么？

	表达式:是计算的过程
	语句:是逻辑操作的过程

	答：一个表达式会产生一个值，它可以放在任何需要一个值的地方；而语句是一段可执行代码，可以理解为一个行为，语句不一定有值。

4. 下边代码中的表达式 a = 0 && 1 = b 编译会出错，你知道为什么吗

	1为常数,并不是变量,而且1也无法定义成变量

5. 下边有一段代码，请描述表达式 a < b && c < d 的求值顺序（先求什么、再求什么、最后求什么）

	a>b为假,&&后条件不会被执行.
6. 请用小括号和数字序号体现表达式中运算符的优先顺序，并口算出表达式的值（下边假设变量 a、b、c、d 的值分别是 1、2、3、4）

	A. a+b && c/d == 1
		1.a+b
		2.c/d
	B. a=b && c-d == 1
		1.a=b
		2.c-d
	C. 'a'+'b' <= 'c' || a/b+d%c == 1
		1.'a'+'b'
		2.'c'
		3.<=
		4.a/b
		5.d%c
		6.a/b+d%c
		7.||
	D. !!a+!b + c -a -c == 0
		1.!!a
		2.!b
		3.!!a+!b
		4.!!a+!b+c
		5.!!a+!b+c-a
		6.!!a+!b+c-a-c
	E.a=b-c||a % b && c/d == 0
		1.b-c
		2.a=b-c
	
7. 请说明逻辑与（&&）在下边表达式中起到的重要作用（这道题很经典，不要轻易看答案(⊙o⊙)哦 

	count != 0 && sum/count
	当count 不为0为真时 -- 会执行sum/count
	当count 为假时 -- 结束
	其目的是保证count分母不为0

8. 请用一个逻辑表达式来判断某一年是否为闰年。
	#include <stdio.h>
	int main(){
		int year;
		scanf("%d",&year);
		if (year%4 == 0 && year % 100 != 0 || year % 400 == 0) // 逻辑表达式在这
			printf("1");
		else
			printf("0");

		return 0;
	}
	
	

